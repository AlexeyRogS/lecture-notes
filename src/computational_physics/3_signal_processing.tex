\documentclass[../computational_physics.tex]{subfiles}

\begin{document}

    \section{Signal Processing}
    % # average frequency of a signal
    % from scipy.signal import find_peaks

    % peaks, info = find_peaks(data, prominence=10)
    % # gives the indices of the peaks

    % measured_periods = np.diff(peaks)
    % avg_measured_period = np.average(measured_periods)
    % avg_freq = 1 / avg_measured_periods

    % talk about writing a continuous time series in terms of a fourier series (assume the function is period outside the sampling interval and use the DFT)
    % noise filtering (threshold filters, bandpass filters)
    % amplitude modulation

\end{document}
