% !TeX root = ..\real_analysis.tex
\documentclass[../computational_physics.tex]{subfiles}

\begin{document}

    \section{Symbolic Computation}
    % # find the eigenvalues of a matrix

    % import sympy
    % sympy.factor(A.charpoly().as_expr())

    % # for non integer roots
    % roots = [(float(sympy.re(x)), float(sympy.im(x))) for x in A.charpoly().all_roots()]
    
    % talk about solving geometric simple optics problems with ray transfer matrices
    % find eigenvalues of the matrix to see which rays will converge and which will diverge

\end{document}
