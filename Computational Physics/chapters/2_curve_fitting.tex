% !TeX root = ..\real_analysis.tex
\documentclass[../computational_physics.tex]{subfiles}

\begin{document}

    \section{Curve Fitting}
        % import numpy as np
        % from scipy import optimise
        
        % optimise.curve_fit(fitting_function, x, y, p0)
        % # p0 is an array of initial guesses for parameters
        % # fitting_function is a Python function f(x, ...params) that we are trying to fit the data to
        % # returns (optimal_parameters, estimated_covariance)

        % estimated_std_dev = np.sqrt(np.diag(estimated_covariance))

        % implement Newton-Raphson method in Fortran
\end{document}
