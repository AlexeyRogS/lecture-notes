% !TeX root = ..\real_analysis.tex
\documentclass[../computational_physics.tex]{subfiles}

\begin{document}

    \section{Basics}    
    % introduction to Python libaries (cmath, NumPy, SymPy, SciPy, Pandas, Matplotlib), floating point calculations, plotting

    % Fortran cheat sheet, style guide (each source file should have one module which matches filename)

    % finding the length of a file in Fortran
    % INTEGER :: length, ioerr
    % REAL, DIMENSION(:), ALLOCATABLE :: arr
    % length = 0
    % OPEN(1, file="filename", status="old")
    % DO
    %     READ(1, +, IOSTAT=ioerr)
    %     IF (ioerr /= 0) EXIT
    %     length = length + 1
    % END DO
    % CLOSE(1)
    % ALLOCATE(arr(length), ...)

\end{document}
